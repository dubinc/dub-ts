/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as z from "zod";

/**
 * A short code indicating the error code returned.
 */
export enum ForbiddenCode {
    Forbidden = "forbidden",
}

export type ForbiddenError = {
    /**
     * A short code indicating the error code returned.
     */
    code: ForbiddenCode;
    /**
     * A human readable explanation of what went wrong.
     */
    message: string;
    /**
     * A link to our documentation with more details about this error code
     */
    docUrl?: string | undefined;
};

/**
 * The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server.
 */
export type ForbiddenData = {
    error: ForbiddenError;
};

/**
 * The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server.
 */
export class Forbidden extends Error {
    error: ForbiddenError;

    /** The original data that was passed to this error instance. */
    data$: ForbiddenData;

    constructor(err: ForbiddenData) {
        super("");
        this.data$ = err;

        this.error = err.error;

        this.message =
            "message" in err && typeof err.message === "string"
                ? err.message
                : "API error occurred";

        this.name = "Forbidden";
    }
}

/** @internal */
export const ForbiddenCode$ = z.nativeEnum(ForbiddenCode);

/** @internal */
export namespace ForbiddenError$ {
    export type Inbound = {
        code: ForbiddenCode;
        message: string;
        doc_url?: string | undefined;
    };

    export const inboundSchema: z.ZodType<ForbiddenError, z.ZodTypeDef, Inbound> = z
        .object({
            code: ForbiddenCode$,
            message: z.string(),
            doc_url: z.string().optional(),
        })
        .transform((v) => {
            return {
                code: v.code,
                message: v.message,
                ...(v.doc_url === undefined ? null : { docUrl: v.doc_url }),
            };
        });

    export type Outbound = {
        code: ForbiddenCode;
        message: string;
        doc_url?: string | undefined;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, ForbiddenError> = z
        .object({
            code: ForbiddenCode$,
            message: z.string(),
            docUrl: z.string().optional(),
        })
        .transform((v) => {
            return {
                code: v.code,
                message: v.message,
                ...(v.docUrl === undefined ? null : { doc_url: v.docUrl }),
            };
        });
}

/** @internal */
export namespace Forbidden$ {
    export type Inbound = {
        error: ForbiddenError$.Inbound;
    };

    export const inboundSchema: z.ZodType<Forbidden, z.ZodTypeDef, Inbound> = z
        .object({
            error: z.lazy(() => ForbiddenError$.inboundSchema),
        })
        .transform((v) => {
            return new Forbidden({
                error: v.error,
            });
        });

    export type Outbound = {
        error: ForbiddenError$.Outbound;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, Forbidden> = z
        .instanceof(Forbidden)
        .transform((v) => v.data$)
        .pipe(
            z
                .object({
                    error: z.lazy(() => ForbiddenError$.outboundSchema),
                })
                .transform((v) => {
                    return {
                        error: v.error,
                    };
                })
        );
}
