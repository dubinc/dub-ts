/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type DiscountCodeSchema = {
  /**
   * The unique ID of the discount code.
   */
  id: string;
  /**
   * The alphanumeric discount code that customers can apply at checkout.
   */
  code: string;
  /**
   * The ID of the discount this code belongs to.
   */
  discountId: string | null;
  /**
   * The ID of the partner this discount code is assigned to.
   */
  partnerId: string;
  /**
   * The ID of the partner's referral link this discount code is associated with.
   */
  linkId: string;
  /**
   * When this discount code was disabled, which happens when a partner is banned or deactivated. We don't delete the discount code to avoid another partner claiming a banned/deactivated code (abuse vector).
   */
  disabledAt?: string | null | undefined;
};

/** @internal */
export const DiscountCodeSchema$inboundSchema: z.ZodType<
  DiscountCodeSchema,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  code: z.string(),
  discountId: z.nullable(z.string()),
  partnerId: z.string(),
  linkId: z.string(),
  disabledAt: z.nullable(z.string()).optional(),
});

export function discountCodeSchemaFromJSON(
  jsonString: string,
): SafeParseResult<DiscountCodeSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DiscountCodeSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DiscountCodeSchema' from JSON`,
  );
}
