/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type AnalyticsEventNames = {
  /**
   * The name of the conversion event (lead or sale)
   */
  eventName: string;
  /**
   * The number of clicks from this event name
   */
  clicks: number;
  /**
   * The number of leads from this event name
   */
  leads: number;
  /**
   * The number of sales from this event name
   */
  sales: number;
  /**
   * The total amount of sales from this event name, in cents
   */
  saleAmount: number;
};

/** @internal */
export const AnalyticsEventNames$inboundSchema: z.ZodType<
  AnalyticsEventNames,
  z.ZodTypeDef,
  unknown
> = z.object({
  eventName: z.string(),
  clicks: z.number().default(0),
  leads: z.number().default(0),
  sales: z.number().default(0),
  saleAmount: z.number().default(0),
});

export function analyticsEventNamesFromJSON(
  jsonString: string,
): SafeParseResult<AnalyticsEventNames, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnalyticsEventNames$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnalyticsEventNames' from JSON`,
  );
}
