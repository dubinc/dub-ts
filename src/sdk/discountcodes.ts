/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { discountCodesCreate } from "../funcs/discountCodesCreate.js";
import { discountCodesDelete } from "../funcs/discountCodesDelete.js";
import { discountCodesList } from "../funcs/discountCodesList.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class DiscountCodes extends ClientSDK {
  /**
   * List discount codes
   *
   * @remarks
   * Retrieve a paginated list of discount codes for a partner / a given discount / the whole program.
   */
  async list(
    request?: operations.ListDiscountCodesRequest | undefined,
    options?: RequestOptions,
  ): Promise<Array<components.DiscountCodeSchema>> {
    return unwrapAsync(discountCodesList(
      this,
      request,
      options,
    ));
  }

  /**
   * Create a discount code
   *
   * @remarks
   * Create a discount code for a partner. The partner's group must already have a discount assigned to it, and the discount code must be associated with a link that is not already linked with another discount code.
   */
  async create(
    request?: operations.CreateDiscountCodeRequestBody | undefined,
    options?: RequestOptions,
  ): Promise<components.DiscountCodeSchema> {
    return unwrapAsync(discountCodesCreate(
      this,
      request,
      options,
    ));
  }

  /**
   * Delete a discount code
   *
   * @remarks
   * Delete a discount code for a partner by its unique ID or alphanumeric code. This will also disable the code in your connected discount provider (Stripe, Shopify, or custom via `disccount.deleted` webhook).
   */
  async delete(
    idOrCode: string,
    options?: RequestOptions,
  ): Promise<operations.DeleteDiscountCodeResponseBody> {
    return unwrapAsync(discountCodesDelete(
      this,
      idOrCode,
      options,
    ));
  }
}
