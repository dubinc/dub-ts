/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { tagsUpdate } from "../../funcs/tagsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  id: z.string(),
  requestBody: operations.UpdateTagRequestBody$inboundSchema.optional(),
};

export const tool$tagsUpdate: ToolDefinition<typeof args> = {
  name: "tags-update",
  description: `Update a tag

Update a tag in the workspace.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tagsUpdate(
      client,
      args.id,
      args.requestBody,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
