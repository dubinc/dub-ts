/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { partnersList } from "../../funcs/partnersList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPartnersRequest$inboundSchema.optional(),
};

export const tool$partnersList: ToolDefinition<typeof args> = {
  name: "partners-list",
  description: `List all partners

List all partners for a partner program.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await partnersList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
