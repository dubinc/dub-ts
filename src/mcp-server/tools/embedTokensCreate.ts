/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { embedTokensCreate } from "../../funcs/embedTokensCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateEmbedTokenRequestBody$inboundSchema.optional(),
};

export const tool$embedTokensCreate: ToolDefinition<typeof args> = {
  name: "embed-tokens_create",
  description: `Create a new embed token

Create a new embed token for the referral link.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await embedTokensCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
