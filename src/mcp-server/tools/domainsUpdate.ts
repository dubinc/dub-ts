/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { domainsUpdate } from "../../funcs/domainsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  slug: z.string(),
  requestBody: operations.UpdateDomainRequestBody$inboundSchema.optional(),
};

export const tool$domainsUpdate: ToolDefinition<typeof args> = {
  name: "domains_update",
  description: `Update a domain

Update a domain for the authenticated workspace.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await domainsUpdate(
      client,
      args.slug,
      args.requestBody,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
